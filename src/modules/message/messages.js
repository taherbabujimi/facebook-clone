const messages = {
  errorGettingHistory: "Error while getting message history.",
  userNotExists: "User with this ID does not exists.",
  historyFetchedSuccess: "History fetched successfully.",
};

module.exports = {
  messages,
};
